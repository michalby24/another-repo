name: Trigger External Workflow

on:
  push:
    branches:
      - main

env:
  HELM_EXPERIMENTAL_OCI: 1

permissions:
  contents: write
  pull-requests: write

jobs:
  reusable_workflow_job:
    uses: michalby24/demo-shared-workflow/.github/workflows/build_and_push.yaml@main
    secrets: inherit