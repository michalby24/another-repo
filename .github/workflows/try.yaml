name: Trigger External Workflow

on:
  push:
    branches:
      - main

env:
  HELM_EXPERIMENTAL_OCI: 1

permissions:
  contents: write
  pull-requests: write

jobs:
  trigger-external:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger External Workflow
        uses: michalby24/github-actions